<h1>Show</h1>

<p><%= @task.title%></p>
<p><%= @task.details%></p>
<p><%= @task.completed%></p>
  <%= link_to 'Edit', task_edit_path(@task.id), class: 'btn btn-primary' %>
  <%= link_to "remove", task_delete_path(@task.id), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%>
