<h1>Index</h1>
<%@tasks.each do |task|%>
  <p><%= link_to "#{task.title}", task_path(task.id)%></p>
  <input type="checkbox" <%="checked" if task.completed == true %>/>
<%end%>

  <%= link_to 'Add new task', task_new_path, class: 'btn btn-primary' %>
